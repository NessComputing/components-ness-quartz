# Make sure that the short one is enough
ness.job.running-short.enabled=false
ness.job.running-long.enabled=true

# explicit tested
ness.job.explicit-never-run.enabled=false
ness.job.explicit-running.enabled=true
